import { IconStatePipe } from './icon-state.pipe';

describe('IconStatePipe', () => {
  it('create an instance', () => {
    const pipe = new IconStatePipe();
    expect(pipe).toBeTruthy();
  });
});
